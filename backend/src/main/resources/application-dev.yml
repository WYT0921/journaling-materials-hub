# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://111.229.148.112:3306/journaling_materials_hub?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 0921Txwjp@
  data:
    redis:
      host: localhost
      port: 6379
      password:

# 开发环境日志级别
logging:
  level:
    root: INFO
    com.journaling.hub: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

# 开发环境关闭 API 文档认证
springdoc:
  swagger-ui:
    path: /swagger-ui.html
