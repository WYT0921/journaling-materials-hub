# 开发环境配置

# 管理员配置（用户 ID 列表，逗号分隔）
admin:
  user-ids: 1,6

# MinIO 配置（指向服务器）
minio:
  endpoint: http://111.229.148.112:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: materials

spring:
  datasource:
    url: jdbc:mysql://111.229.148.112:3306/journaling_materials_hub?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 0921Txwjp@
  data:
    redis:
      host: 111.229.148.112
      port: 6379
      password: redis123
#微信小程序appid
wechat:
  appid: wxf174a3d18f0d069b
  secret: c6056fa1d1dc973d2c6ec80134911a8d

# 开发环境日志级别
logging:
  level:
    root: INFO
    com.journaling.hub: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

# 开发环境关闭 API 文档认证
springdoc:
  swagger-ui:
    path: /swagger-ui.html
